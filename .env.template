# ═══════════════════════════════════════════════════════════════════════════════
# Banking ML Platform — Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Copy to .env and customize. All variables use the BANKING_ prefix.
# Only BANKING_API_KEY and BANKING_ENCRYPTION_KEY are recommended for production.

# ── Paths ─────────────────────────────────────────────────────────────────────
# BANKING_BASE_DIR=/path/to/project          # Default: project root
# BANKING_USE_CASES_DIR=/path/to/usecases    # Default: <base>/5_Star_UseCases
# BANKING_OUTPUT_DIR=/path/to/output         # Default: <base>/preprocessing_output
# BANKING_LOGS_DIR=/path/to/logs             # Default: <base>/logs

# ── Database Paths (SQLite) ───────────────────────────────────────────────────
# BANKING_ADMIN_DB=/path/to/admin.db         # Default: <base>/admin.db
# BANKING_UNIFIED_DB=/path/to/unified.db     # Default: <base>/banking_unified.db
# BANKING_RESULTS_DB=/path/to/results.db     # Default: <base>/ml_pipeline_results.db
# BANKING_PREPROCESSING_DB=/path/to/prep.db  # Default: <base>/preprocessing_results.db
# BANKING_RAG_CACHE_DB=/path/to/rag.db       # Default: <base>/rag_cache.db
# BANKING_VECTOR_STORE_DIR=/path/to/vectors  # Default: <base>/vector_store

# ── Security ──────────────────────────────────────────────────────────────────
# API key for admin endpoints. If not set, auth is disabled (dev mode).
# BANKING_API_KEY=your-secret-api-key-here

# Fernet encryption key for integration secrets. Auto-generated if not set.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# BANKING_ENCRYPTION_KEY=

# ── CORS ──────────────────────────────────────────────────────────────────────
# JSON array of allowed origins.
# BANKING_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# ── LLM / Ollama ─────────────────────────────────────────────────────────────
# BANKING_OLLAMA_BASE_URL=http://localhost:11434
# BANKING_OLLAMA_MODEL=llama3.2

# ── Processing ────────────────────────────────────────────────────────────────
# BANKING_SAMPLE_LIMIT=500000
# BANKING_MAX_WORKERS=8
# BANKING_MAX_UPLOAD_SIZE=524288000            # 500MB in bytes

# ── Logging ───────────────────────────────────────────────────────────────────
# BANKING_LOG_LEVEL=INFO
